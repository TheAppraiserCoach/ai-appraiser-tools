# Journal — shared memory across models

> Newest entry first. This is how memory carries between Claude, ChatGPT, Gemini, and
> whatever comes next. Any model reads the top entries and knows what was recently going on.

## Protocol for any AI reading this

- **Starting a session:** read the top few entries plus the brain files, so you already
  know the context. If I reference "what we talked about," it's here.
- **Ending a session:** append a short dated entry — date, which tool, what was discussed,
  what was decided, what's still open. Keep it tight. This is a memory aid, not a
  transcript.

---

## [YYYY-MM-DD] — [which AI you were using]
- [What was discussed, decided, or left open.]
